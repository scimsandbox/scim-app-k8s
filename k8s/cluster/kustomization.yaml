apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - storage
  - cloudflared

labels:
  - pairs:
      app.kubernetes.io/managed-by: kustomize
      app.kubernetes.io/environment: netcup