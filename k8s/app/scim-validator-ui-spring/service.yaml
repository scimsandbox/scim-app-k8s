apiVersion: v1
kind: Service
metadata:
  name: scim-validator-ui-spring
  labels:
    app.kubernetes.io/name: scim-validator-ui-spring
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: scim-validator-ui-spring
  ports:
    - name: http
      port: 8080
      targetPort: http